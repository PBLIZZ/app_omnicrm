# HTTP Schemas File Eliminated

This file contained HTTP envelope validation schemas that have been eliminated in favor of direct patterns.

## Replacements

Instead of importing these schemas, use direct validation:

```typescript
// ❌ Old pattern
import { okEnvelopeSchema, errorEnvelopeSchema, apiResponseSchema } from "./http";

// ✅ Direct pattern
const okSchema = <T extends z.ZodTypeAny>(data: T) => z.object({
  ok: z.literal(true),
  data,
});

const errorSchema = z.object({
  ok: z.literal(false),
  error: z.string(),
  details: z.unknown().optional(),
});
```

## File eliminated:
- Removed Zod schema abstractions
- All usage replaced with direct validation patterns
- Schemas index updated to remove exports

Date: 2025-01-22